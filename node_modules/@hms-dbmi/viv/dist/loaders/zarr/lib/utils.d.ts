import type { ZarrArray } from 'zarr';
import type { OMEXML } from '../../omexml';
import type { RootAttrs } from '../ome-zarr';
export declare function guessBioformatsLabels({ shape }: ZarrArray, { Pixels }: OMEXML[0]): import("../../../types").Labels<["t", "c", "z"] | ["c", "t", "z"] | ["z", "t", "c"] | ["t", "z", "c"] | ["z", "c", "t"] | ["c", "z", "t"]>;
export declare function getRootPrefix(files: {
    path: string;
}[], rootName: string): string;
export declare function loadMultiscales(store: ZarrArray['store'], path?: string): Promise<{
    data: ZarrArray[];
    rootAttrs: RootAttrs;
}>;
export declare function guessTileSize(arr: ZarrArray): number;
